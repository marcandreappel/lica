class LicaWeek {

    constructor(options) {
        if (typeof options === "object") this.options = options || {}
    }

    week() {
        if (this.options.has('limitDays') && Array.isArray(this.options.limitDays === "array"))
    }
}